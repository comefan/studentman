<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.fan.dao.ReserveDao">
    <select id="findAll" resultType="com.fan.entity.Reserve">
        select a.*,b.name as hotelName,c.name as userName from reserve a left join hotel b on a.hotelId = b.id left join user c on a.userId = c.id
        <where>
            <if test="userDTO != null and userDTO.name != null and userDTO.name != ''">
                and b.name like concat('%', #{userDTO.name}, '%')
            </if>
        </where>
        order by a.id desc
    </select>
</mapper>